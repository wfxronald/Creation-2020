<%= form_with(model: challenge_statement, local: true) do |form| %>
  <% if challenge_statement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(challenge_statement.errors.count, "error") %> prohibited this challenge_statement from being saved:</h2>

      <ul>
        <% challenge_statement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :zip %>
    <%= form.file_field :zip, direct_upload: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
